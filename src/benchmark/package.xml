<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>benchmark</name>
  <version>0.1.0</version>
  <description>
    Gazebo benchmark: randomly generates a flat map with gate/pillar obstacles
    (as a world .sdf) for testing the SUPER planner under obstacle-dense
    environments. Density is controlled by discretizing the map into an NxN
    grid and setting the number of objects per cell; a clearing distance keeps
    a minimum gap between obstacles.
  </description>
  <maintainer email="windshape@example.com">windshape</maintainer>
  <license>Apache-2.0</license>

  <exec_depend>python3-yaml</exec_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
